# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI workflow

on:
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file pom.xml
      continue-on-error: true
    - name: Test
      run: mvn test
    - name: Report bugs
      if: failure()
      env:
        OPS_SECRET: ${{ secrets.AZURE_PAT }}
      run: |
        curl -X POST \
          -u 01169585:$OPS_SECRET \
          -d '[{"op":"add","path":"/fields/System.Title","from":null,"value":"Example bug"}]' \
          https://dev.azure.com/01169585/mwo-project/_apis/wit/workitems/Bug?api-version=7.2-preview.3

